#include <stdio.h>

int main() {
  int base;
  int hauteur;
  printf("TRIANGLE \n");
  printf("base: ");
  scanf("%d", &base);
  printf("hauteur: ");
  scanf("%d", &hauteur);
  printf("%d %d\n", base, hauteur);
  return 0;
}
