#include <stdio.h>

int main() {
  int base;
  int hauteur;
  printf("TRIANGLE \n");
  printf("base: ");
  scanf("%d\n", base);
  printf("hauteur: ");
  scanf("%d\n", hauteur);
printf("%d %d\n", base, hauteur);
  return 0;
}
